Flask==3.0.3
flask-cors==4.0.1

# Auth
bcrypt==4.2.0
PyJWT==2.8.0

# Crypto / runtime support
cryptography==43.0.1
python-dotenv==1.0.1
werkzeug==3.0.3

# App server (local / non-Vercel production use)
gunicorn==23.0.0

psycopg2-binary==2.9.9    # PostgreSQL
# pymongo==4.7.2            # MongoDB alternative
