"""EduGuard API library package.

The package is intentionally kept free of eager re-exports so importing a
submodule such as ``api.lib.env`` does not trigger unrelated application
imports and circular dependencies.
"""